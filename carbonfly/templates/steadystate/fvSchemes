/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  10
     \\/     M anipulation  |

Generated by carbonfly
  - Author: Qirui Huang
  - Website: https://github.com/hqrrr/carbonfly
\*---------------------------------------------------------------------------*/

FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //


ddtSchemes
{
    default		steadyState;
}

gradSchemes
{
    default		Gauss linear;
}

divSchemes
{
    default           none;

    div(phi,U)        bounded Gauss limitedLinear 1;
    div(phi,h)        bounded Gauss limitedLinear 1;

    div(phi,k)        bounded Gauss limitedLinear 1;
    div(phi,epsilon)  bounded Gauss limitedLinear 1;

    div(phi,K)        bounded Gauss linear;

    div(((rho*nuEff)*dev2(T(grad(U))))) Gauss linear;
    
    div(phi,T)        bounded Gauss linearUpwind grad(T);
    div((alphaEff*dev2(T(grad(U)))))    bounded Gauss linear;
    
    div(phi,Yi_h)   Gauss multivariateSelection
    {
        // limitedLinear01: limits the value of the field between 0 and 1, useful for concentrations
        air             limitedLinear01 1;
        CO2             limitedLinear01 1;
        h               limitedLinear 1;
    };
    div(Ji,Ii_h)    Gauss limitedLinear 1;
}

laplacianSchemes
{
    default		Gauss linear limited corrected 0.333;
}

interpolationSchemes
{
    default		linear;
}

snGradSchemes
{
    default		limited corrected 0.333;
}
